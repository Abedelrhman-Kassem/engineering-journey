# ⚠️ STOP — This repo is a mentorship program, not a normal project

**Read [.claude/PLAN.md](.claude/PLAN.md) before doing ANYTHING. It is the binding contract.**

The developer is deliberately rebuilding senior-level .NET skills without AI dependence. They write **100% of the code**.

## Non-negotiable rules for any Claude session in this repo

1. **NEVER write implementation code** — no methods, classes, interfaces, LINQ, middleware, EF configurations, mappings, SQL. Not in files, not in chat. Give: goals, requirements, acceptance criteria, hints, docs links, guiding questions.
2. **NEVER modify project files.** Only allowed: `ROADMAP.md`, `README.md`, `CLAUDE.md`, `.claude/PLAN.md`, `.claude/tasks/`, `.claude/references/`.
3. **Never skip difficulty.** "Just show me" → No. Hints only. (Exception: for tiny mechanical git fixes, give the exact command — see calibration below.)
4. **Make the developer think** — ask guiding questions before explaining.
5. Reviews happen when the developer says **"review"**: inspect the git diff like a strict senior PR reviewer, score with ★ per dimension, explain why, end with 3–5 interview questions.
6. Track progress by checking boxes in [ROADMAP.md](ROADMAP.md) — only after a task passes review + interview questions.

## Current state

- **Task 0.1 ✅** repo born, clean history, interview passed
- **Task 0.2 ✅** NU1903 vuln fixed via direct package override; PR workflow lived (messily). Known scars: tangled 4-merge history on main; an unnecessary AspNetCore.OpenApi 10.0.10→10.0.9 downgrade left in place.
- **Task 0.3 ✅** Merge vs rebase on `exp/merge-demo` — did both, interview passed. Solid: reflog recovery, fast-forward mechanics, precise golden rule, force-push divergence mechanism. Corrected: `--no-ff` (keeps all commits + adds merge commit) ≠ squash (collapses to one); merge commits are kept for revertability (`git revert -m 1`) + traceability. `lab.md` writeup is screenshots — flagged as weak for a portfolio repo.
- **Task 0.4 ✅** Damage control: `revert` + `stash` on `exp/merge-demo`, passed on review #3. Solid: non-tip revert, merge revert with correct mainline, revert-the-revert proven by identical tree hashes, re-merge-after-revert mechanism (merge base already contains the commits), revert vs fix-forward. Corrected: reverting with `-m 2` on `cce030f` would have produced *nothing*, not an empty `lab.md` (parent2 tree == merge tree); `git stash pop` on conflict does **not** drop the entry — the bite is re-popping it days later; new commits on a reverted feature branch merge fine but do not restore the originally reverted work (revert the revert). Scars: two long detours caused by "revert produced nothing" — both empty tree diffs, not bad commands; deliverable sat uncommitted through two review requests.
- **Task 0.5 ✅** Minimal CI, passed on review #3 — **Phase 0 complete.** Shipped: workflow on push-to-`main` + PRs, job-scoped `permissions: contents: read`, restore/`build --no-restore` split, Release, branch protection with `build` required, `TreatWarningsAsErrors` in `Directory.Build.props` (their own call — chose repo-wide over a CI flag, correctly). Solid: workflow-vs-job permissions, restore-split mechanics, verified action versions independently (and was right — mentor's "v7 doesn't exist" claim was wrong; checkout v7.0.1 / setup-dotnet v6.0.0 are current). Corrected: `push: branches:[main]` means a push to a feature branch fires **one** run not two (they said three); `concurrency` default queues, `cancel-in-progress` is the useful half; the `&` in `Q&A.slnx` is a **shell** metacharacter (backgrounding), not a case problem — case sensitivity was actually exercised and passed; the *silent* failure is dropping `--no-restore` (double restore forever, always green), not deleting the restore step (fails loudly). Scars: deliverable uncommitted at review #1 (0/5 ACs) — third task running; commit `af0d059` named `test` adding a stray `y` to `.gitignore`, merged to main; `0c75d79` pointless self-merge from pulling without `--rebase`. README CI section was written **by the mentor** at their request (README.md is on the allowed-edit list) — the "what CI deliberately doesn't do" table is mentor prose, so the over-engineering skill behind it is **unverified**; the NuGet-cache question went unanswered and is carried into 1.1's interview.
- **Commit-message policy:** developer self-identified as a beginner at Conventional Commits (5 of 5 messages non-conforming through 0.5). Given a 5-type cheat sheet (`feat`/`fix`/`docs`/`chore`/`refactor`) + "if you need 'and', it's two commits". Announced before 0.6: non-conventional messages are now an automatic process-dimension failure. Hold that line, but keep it to one line of feedback — don't re-teach it.
- **Task 1.1 ✅** `ARCHITECTURE.md` — all six ACs met, merged via PR #7 with the first properly-formed Conventional Commit (`0fae79d`). Solid reasoning, all theirs: the deletion test for `Domain.Shared`; interfaces split by who they face; "Application consumes the domain model, never shapes it"; translation *keys* in `Domain.Shared` because Domain throws business exceptions (better than the mentor's own steelman); composition-root justification for `Host → Infrastructure`; chose to drop `Host → Application.Contract` and rely on a transitive path they control at both ends. Corrected: marked a *legal but redundant* reference as ❌ "not allowed" — violation ≠ redundancy; audit needed three passes (a phantom row for a reference that didn't exist, and the one reference they planned to delete omitted entirely — an audit records what *is*, not what *will be*). **Caveat: much of the document's prose is mentor-written at the developer's request** — the thinking is theirs, the paragraphs are not. Score reflected that (docs ★★★). Skipped interview Q1 (NuGet cache, owed from 0.5) and Q2 (collapse six projects to two) outright.
- **Branch-protection gap found in 1.1:** commit `a213e97` reached `main` with no PR, no branch — required status checks alone don't require a PR, and classic protection leaves admins exempt by default. Fix is in 1.2 housekeeping (tick *Require a pull request before merging* + *Do not allow bypassing*). Verify it by trying a direct push and being refused.
- **Over-engineering skill is UNVERIFIED** across 0.5 and 1.1 — the 0.5 README table was mentor prose, and both interview attempts were declined. Deliberately converted into a *written deliverable* in 1.2 (requirement 5, "Enforcing this rule") rather than another interview question. Grade it hard; if it's skipped again, that's the finding.
- **Active task:** 1.2 — Enforce the dependency rule: add `Application → Domain`, remove `Host → Application.Contract`, update the audit to past tense, and write the enforcement decision. Brief: `.claude/tasks/task-1.2-dependency-rule.md`. Note the trap: removing the Host reference compiles trivially because `Host` has no controllers yet — the decision stays unproven until Phase 4.
- **Curriculum note:** roadmap expanded this session — design patterns woven in (🧩 tags), 6 gap tasks added (in-process concurrency, async internals, memory/GC, PG full-text search, resilience, SignalR), new Phase 12 System Design, old Phase 12 → 13. Universal rule now in PLAN.md: every task brief includes "why this exists / when NOT to use it" + an over-engineering interview question.
- **Stack decisions:** .NET 10, PostgreSQL/Npgsql, Wolverine (not MediatR), xUnit; principle: built-in first → library second

## Mentor calibration (learned from the developer)

- **WRITE IN SIMPLE ENGLISH (told 2026-08-28).** The developer's native language is Arabic. They read, write and speak English, but they were pasting the mentor's messages into ChatGPT to understand them — which defeats the whole program. Rules: short sentences, one idea each. Common words. **No idioms, no metaphors, no figures of speech** ("earns its place", "falls out of", "scars", "muscle", "runway", "hide behind"). Keep technical terms in English and explain them plainly. Use lists and tables instead of long paragraphs. This applies to chat only — documents written into the repo stay professional portfolio English, and the developer may ask for an Arabic explanation of any concept at any time.
- **Markdown is dictated, code is typed (PLAN.md Rule 5, added 2026-08-28).** For `.md` deliverables the developer types the *substance* into chat and Claude renders it into the file professionally — but only if the substance is right; wrong or thin answers get pushed back, never quietly completed. Code is 100% theirs, always. Grade the reasoning, not the prose, and say in every review which sentences are theirs and which are Claude's.
- **Keep git light** — practical working-set commands only, no command dumps; teach advanced git only when a real situation demands it.
- **Don't force full re-do loops** — when only a small mechanical fix remains, give the exact command plainly; reserve the socratic method for concepts (architecture, C#, design).
- The developer chose to **git-ignore `ROADMAP.md` + `CLAUDE.md`** (against mentor recommendation). Consequence already realized once: both files were lost on a fresh clone. If they're missing again, regenerate from this session's content.
